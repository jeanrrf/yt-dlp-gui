# Skills folder

Place skill directories here. Each skill folder should contain a `SKILL.md` describing purpose, usage, and compatibility.

Example:

skills/text-normalizer/
  SKILL.md
  example.md

Conventions:
- `meta.json` optional with `provider`, `version`, `tags`.
- Keep each `SKILL.md` concise and include examples.
